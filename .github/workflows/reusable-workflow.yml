name: Reusable Workflow Example

on: [workflow_call]

jobs:
  example_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo "Welcome to Infoane technologies!!!"
